# Runtara Core Configuration

# PostgreSQL connection (required)
RUNTARA_DATABASE_URL=postgres://runtara:password@localhost/runtara

# QUIC server ports
RUNTARA_QUIC_PORT=7001
RUNTARA_ADMIN_PORT=7002

# Logging
RUST_LOG=runtara_core=info
