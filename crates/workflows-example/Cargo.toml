[package]
name = "workflows-example"
version = "1.0.24"
edition = "2021"
license = "AGPL-3.0-or-later"
description = "Example of generating, compiling, and running workflows with runtara-workflows"
publish = false

[[bin]]
name = "workflows-example"
path = "src/main.rs"

[dependencies]
runtara-workflows = { path = "../runtara-workflows" }
runtara-dsl = { path = "../runtara-dsl" }
serde_json = "1.0"
tempfile = "3.23"
