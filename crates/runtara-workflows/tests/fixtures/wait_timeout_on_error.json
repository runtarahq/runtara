{
  "name": "Wait Timeout onError",
  "description": "GAP-14: a WaitForSignal timeout expiry routes to the step's onError handler with the structured WAIT_TIMEOUT envelope",
  "steps": {
    "wait": {
      "stepType": "WaitForSignal",
      "id": "wait",
      "name": "Approval",
      "timeoutMs": {
        "valueType": "immediate",
        "value": 1
      },
      "pollIntervalMs": 0
    },
    "finish": {
      "stepType": "Finish",
      "id": "finish",
      "inputMapping": {
        "approved": {
          "valueType": "reference",
          "value": "steps.wait.outputs.approved"
        }
      }
    },
    "handler_finish": {
      "stepType": "Finish",
      "id": "handler_finish",
      "inputMapping": {
        "handled": { "valueType": "immediate", "value": true },
        "code": { "valueType": "reference", "value": "steps.__error.code" },
        "category": { "valueType": "reference", "value": "steps.__error.category" }
      }
    }
  },
  "entryPoint": "wait",
  "executionPlan": [
    {
      "fromStep": "wait",
      "toStep": "finish"
    },
    {
      "fromStep": "wait",
      "toStep": "handler_finish",
      "label": "onError"
    }
  ],
  "variables": {},
  "inputSchema": {},
  "outputSchema": {}
}
