{
  "name": "Split Timeout",
  "description": "Direct-emitter rejection fixture: a sequential Split with a non-durable per-item Delay. It must be rejected before launch, because a delay may not retain a runner while the Split timeout is pending.",
  "durable": false,
  "steps": {
    "split": {
      "stepType": "Split",
      "id": "split",
      "name": "Slow Split",
      "config": {
        "value": {
          "valueType": "reference",
          "value": "data.items"
        },
        "sequential": true,
        "timeout": 10
      },
      "subgraph": {
        "name": "Slow Item",
        "entryPoint": "tick",
        "steps": {
          "tick": {
            "stepType": "Delay",
            "id": "tick",
            "name": "Slow step",
            "durationMs": {
              "valueType": "immediate",
              "value": 50
            }
          },
          "finish": {
            "stepType": "Finish",
            "id": "finish",
            "inputMapping": {
              "item": {
                "valueType": "reference",
                "value": "data.item"
              }
            }
          }
        },
        "executionPlan": [
          {
            "fromStep": "tick",
            "toStep": "finish"
          }
        ]
      }
    },
    "finish": {
      "stepType": "Finish",
      "id": "finish",
      "inputMapping": {
        "results": {
          "valueType": "reference",
          "value": "steps.split.outputs"
        }
      }
    }
  },
  "entryPoint": "split",
  "executionPlan": [
    {
      "fromStep": "split",
      "toStep": "finish"
    }
  ],
  "variables": {},
  "inputSchema": {
    "items": {
      "type": "array"
    }
  },
  "outputSchema": {}
}
