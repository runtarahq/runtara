[package]
name = "runtara-agent-object-model"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Object Model CRUD agent — WebAssembly Component"
keywords = ["wasm", "component", "agent", "object-model"]
categories = ["wasm", "database"]

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wit-bindgen = "0.58"
serde = { workspace = true }
serde_json = { workspace = true }
# runtara-http compiles host-side (ureq) and wasm-side (wasi). Object-model
# requests target the *internal* runtara-server object-model API and so call
# `.call()` directly — bypassing `RUNTARA_HTTP_PROXY_URL`. The connection is
# threaded via the `connectionId` query string + JSON body field, and the
# tenant via the `X-Org-Id` header sourced from `RUNTARA_TENANT_ID`.
# Capability metadata stays beside the code: `#[capability_input]`,
# `#[capability_output]`, `#[capability]` emit `&'static CapabilityMeta` /
# `&'static InputTypeMeta` / `&'static OutputTypeMeta` items, and the host-only
# `agent_info()` fn collects them into a `runtara_dsl::agent_meta::AgentInfo`.
runtara-agent-macro = { path = "../../runtara-agent-macro" }
runtara-dsl = { path = "../../runtara-dsl", default-features = false }



[target.'cfg(not(target_family = "wasm"))'.dependencies]
runtara-http = { path = "../../runtara-http", default-features = false, features = ["native"] }

[target.'cfg(target_family = "wasm")'.dependencies]
runtara-http = { path = "../../runtara-http", default-features = false, features = ["wasi"] }
