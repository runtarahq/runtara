{
  "definitionVersion": 1,
  "layout": [
    {
      "id": "n_columns",
      "type": "columns",
      "columns": [
        {
          "id": "col_metrics",
          "width": 0.4,
          "children": [
            { "id": "n_metric", "type": "block", "blockId": "rate_limit_usage" }
          ]
        },
        {
          "id": "col_table",
          "width": 0.6,
          "children": [
            { "id": "n_status", "type": "block", "blockId": "rate_limit_status" }
          ]
        }
      ]
    }
  ],
  "filters": [
    {
      "id": "interval",
      "label": "Interval",
      "type": "select",
      "default": "1h",
      "options": {
        "source": "static",
        "values": [
          { "label": "Last hour", "value": "1h" },
          { "label": "Last 24 hours", "value": "24h" },
          { "label": "Last 7 days", "value": "7d" },
          { "label": "Last 30 days", "value": "30d" }
        ]
      }
    }
  ],
  "blocks": [
    {
      "id": "rate_limit_status",
      "type": "table",
      "title": "Rate limit status",
      "source": {
        "kind": "system",
        "entity": "connection_rate_limit_status",
        "mode": "filter",
        "interval": "1h"
      },
      "table": {
        "columns": [
          { "field": "connection_id", "label": "Connection", "descriptive": true },
          { "field": "requests_used", "label": "Used", "format": "number", "align": "right" },
          { "field": "requests_remaining", "label": "Remaining", "format": "number", "align": "right" },
          { "field": "utilization_pct", "label": "Utilization", "format": "percent", "align": "right" },
          {
            "field": "throttled",
            "label": "Throttled",
            "format": "pill",
            "pillVariants": { "true": "warning", "false": "muted" }
          }
        ]
      }
    },
    {
      "id": "rate_limit_usage",
      "type": "metric",
      "title": "Overall utilization",
      "source": {
        "kind": "system",
        "entity": "connection_rate_limit_status",
        "mode": "aggregate",
        "interval": "1h",
        "aggregates": [
          { "alias": "value", "op": "avg", "field": "utilization_pct" }
        ]
      },
      "metric": {
        "valueField": "value",
        "label": "Utilization",
        "format": "percent"
      }
    }
  ]
}
