// Re-exports for analytics module
// Following the same pattern as metrics for consistency

// Re-export DTOs from the new location
pub use crate::api::dto::analytics::*;

// Re-export handlers from the new location
pub use crate::api::handlers::analytics::*;
