{
  "db_name": "PostgreSQL",
  "query": "\n            SELECT\n                hour_bucket,\n                SUM(invocation_count) as invocation_count,\n                SUM(success_count) as success_count,\n                SUM(failure_count) as failure_count,\n                SUM(timeout_count) as timeout_count,\n                CASE\n                    WHEN SUM(invocation_count) > 0\n                    THEN SUM(total_duration_seconds) / SUM(invocation_count)\n                    ELSE 0::double precision\n                END as avg_duration_seconds,\n                CASE\n                    WHEN SUM(invocation_count) > 0\n                    THEN SUM(total_memory_mb) / SUM(invocation_count)\n                    ELSE 0::double precision\n                END as avg_memory_mb,\n                CASE\n                    WHEN SUM(invocation_count) > 0\n                    THEN ROUND((SUM(success_count)::numeric / SUM(invocation_count)::numeric * 100), 2)::double precision\n                    ELSE 0::double precision\n                END as success_rate_percent\n            FROM scenario_metrics_hourly\n            WHERE tenant_id = $1\n              AND hour_bucket >= $2\n              AND hour_bucket < $3\n            GROUP BY hour_bucket\n            ORDER BY hour_bucket ASC\n            ",
  "describe": {
    "columns": [
      {
        "ordinal": 0,
        "name": "hour_bucket",
        "type_info": "Timestamptz"
      },
      {
        "ordinal": 1,
        "name": "invocation_count",
        "type_info": "Int8"
      },
      {
        "ordinal": 2,
        "name": "success_count",
        "type_info": "Int8"
      },
      {
        "ordinal": 3,
        "name": "failure_count",
        "type_info": "Int8"
      },
      {
        "ordinal": 4,
        "name": "timeout_count",
        "type_info": "Int8"
      },
      {
        "ordinal": 5,
        "name": "avg_duration_seconds",
        "type_info": "Float8"
      },
      {
        "ordinal": 6,
        "name": "avg_memory_mb",
        "type_info": "Float8"
      },
      {
        "ordinal": 7,
        "name": "success_rate_percent",
        "type_info": "Float8"
      }
    ],
    "parameters": {
      "Left": [
        "Text",
        "Timestamptz",
        "Timestamptz"
      ]
    },
    "nullable": [
      false,
      null,
      null,
      null,
      null,
      null,
      null,
      null
    ]
  },
  "hash": "27261ed09d79fb54bd87505796d3ebe6ab0ff49ce47f1298c381759af648d73e"
}
