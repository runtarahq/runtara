[package]
name = "runtara-dsl"
version = "0.1.0"
edition = "2024"

[features]
default = []
utoipa = ["dep:utoipa"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
schemars = "0.8"
inventory = "0.3"
utoipa = { version = "5.3", optional = true }
strum = { version = "0.26", features = ["derive"] }
